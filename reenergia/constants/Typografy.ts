import { StyleSheet } from "react-native";

export const fonts = StyleSheet.create({
    xl: { fontSize: 26 },
    l: { fontSize: 22 },
    m: { fontSize: 18 },
    s: { fontSize: 16 },
    xs: { fontSize: 15 },
    bold: { fontWeight: 'bold' },
})

